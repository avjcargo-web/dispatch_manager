import PlaceholderPage from "@/components/ui/PlaceholderPage";

export default function ImportContainersPage() {
  return (
    <PlaceholderPage
      title="Import Containers"
      description="Manage inbound import container workflows, milestones, and assigned handling."
    />
  );
}
